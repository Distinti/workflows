@AGENTS.md

<!-- Pointer file. Claude Code reads CLAUDE.md, not AGENTS.md, so the import
     above inlines this directory's AGENTS.md into context automatically.
     Edit AGENTS.md — never this file. Identical in every directory. -->
